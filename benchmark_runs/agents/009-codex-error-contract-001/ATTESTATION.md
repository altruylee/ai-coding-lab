# Blind-run attestation

> ATTESTATION: no tools, filesystem, network, tests, or reference accessed

The candidate code and this statement are preserved from the isolated agent's
response without correction. The prompt accidentally added an exception-type
contract not present in the original specification-only prompt. The run is
retained as a protocol deviation and excluded from the primary repeated
comparison.
